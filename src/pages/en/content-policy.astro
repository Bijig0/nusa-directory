---
import StaticPageView from '../../views/StaticPageView.astro';
import { resolvePageEntry } from '../../views/routes';
const entry = await resolvePageEntry(Astro, 'content-policy');
if (entry instanceof Response) return entry;
---
<StaticPageView entry={entry} path="/kebijakan-konten/" />
