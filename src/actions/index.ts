import { reveal } from './reveal';
import { favorites } from './favorites';
import { checkout } from './checkout';
import { auth } from './auth';

export const server = { reveal, favorites, checkout, auth };
